<Window x:Class="GymMembershipManager.Views.MembershipTypeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GymMembershipManager.Views"
        mc:Ignorable="d"
        Title="MembershipTypeView" Height="450" Width="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Types}"
          SelectedItem="{Binding SelectedType}"
          AutoGenerateColumns="False"
          HorizontalAlignment="Left" Height="219" Margin="20,20,0,0" 
          VerticalAlignment="Top" Width="370">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naziv" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Cena" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Trajanje (dana)" Binding="{Binding DurationInDays}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
