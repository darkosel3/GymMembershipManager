<Window x:Class="GymMembershipManager.Views.AddMemberView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dodaj člana" Height="270" Width="250" MinWidth="250" MinHeight="270">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ime:"/>
        <TextBox Grid.Row="1" Text="{Binding FirstName}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Prezime:"/>
        <TextBox Grid.Row="3" Text="{Binding LastName}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Text="Broj telefona:"/>
        <TextBox Grid.Row="5" Text="{Binding PhoneNumber}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="6" Text="Datum rođenja:"/>
        <DatePicker Grid.Row="7" SelectedDate="{Binding BirthDate}" Margin="0,0,0,0"/>

        <Button Grid.Row="9" Content="Sačuvaj" Command="{Binding SaveCommand}" Width="100" HorizontalAlignment="Right"/>
    </Grid>
</Window>